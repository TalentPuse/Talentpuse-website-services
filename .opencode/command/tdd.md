---
description: Làm việc test-first — load skill `tdd` (red-green-refactor) cho feature/bug được đưa vào.
agent: build
---

Load skill `tdd` (dùng skill tool — skill nằm ở `.agents/skills/tdd/`) và làm theo đúng workflow red-green-refactor của nó cho:

$ARGUMENTS

Yêu cầu: viết test đỏ TRƯỚC, rồi mới code để test xanh. Báo lại danh sách test đã chạy.
