__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
# Catch every variant, not just the three we happened to think of. A plain `.env`
# rule does not match `.env.bak-r2`, which is how a file with 7 secrets ends up
# sitting untracked, one `git add .` away from being published.
.env*
!.env.example
