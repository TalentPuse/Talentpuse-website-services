---
description: Hỏi xem nên dùng skill/workflow nào cho tình huống hiện tại — load skill `ask-matt`.
agent: build
---

Load skill `ask-matt` (dùng skill tool — skill nằm ở `.claude/skills/ask-matt/`) với tình huống:

$ARGUMENTS

Báo lại skill phù hợp nhất + lý do ngắn gọn, và đề xuất command tương ứng để chạy nó.
