---
description: Implement một ticket/spec — load skill `implement` và làm theo đúng workflow của nó.
agent: build
---

Load skill `implement` (dùng skill tool — skill nằm ở `.agents/skills/implement/`) và làm theo đúng workflow trong skill đó cho công việc sau:

$ARGUMENTS

Làm xong báo lại: file nào đã sửa, cách verify, và kết quả test.
