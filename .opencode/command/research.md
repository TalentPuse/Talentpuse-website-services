---
description: Nghiên cứu một câu hỏi từ nguồn đáng tin cậy, lưu kết quả thành Markdown trong repo — load skill `research`.
agent: build
---

Load skill `research` (dùng skill tool — skill nằm ở `.claude/skills/research/`) và nghiên cứu:

$ARGUMENTS

Chỉ dùng nguồn primary/high-trust, ghi nguồn rõ ràng, kết quả là file Markdown trong repo.
