---
description: Chẩn đoán sự cố CopilotKit (runtime, streaming, tool, AG-UI events) — load skill `copilotkit-debug`.
agent: build
---

Load skill `copilotkit-debug` (dùng skill tool — skill nằm ở `.agents/skills/copilotkit-debug/`) và debug vấn đề:

$ARGUMENTS

Đi theo hướng trong skill: runtime connectivity → agent không trả lời → streaming → tool execution → AG-UI event tracing.
