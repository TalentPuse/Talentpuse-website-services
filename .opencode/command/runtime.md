---
description: Dùng @copilotkit/runtime (server, AgentRunner, tools server-side) — load skill `runtime`.
agent: build
---

Load skill `runtime` (dùng skill tool — skill nằm ở `.agents/skills/runtime/`) và làm việc với CopilotKit runtime:

$ARGUMENTS

Chọn đúng reference trong skill: fetch-native CopilotRuntime, AgentRunner, BuiltInAgent, defineTool, intelligence mode.
