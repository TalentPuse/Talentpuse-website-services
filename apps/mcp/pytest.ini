# pytest 9 doi default import mode sang `importlib` — khong tu them rootdir vao
# sys.path, nen `from mcp_server...` / `from tests.conftest import ...` chet voi
# ModuleNotFoundError (da vap 2026-08-07, CI test job). `pythonpath = .` bat
# pytest them apps/mcp vao sys.path o MOI version (8 va 9).
[pytest]
pythonpath = .
