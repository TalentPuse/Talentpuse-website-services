---
description: Render A2UI surfaces (createSurface/updateComponents/updateDataModel) — load skill `a2ui-renderer`.
agent: build
---

Load skill `a2ui-renderer` (dùng skill tool — skill nằm ở `.claude/skills/a2ui-renderer/`) và làm việc với A2UI:

$ARGUMENTS

Đúng quy trình trong skill: enable runtime a2ui → provider → renderer, theme customization, action-bridge cleanup.
