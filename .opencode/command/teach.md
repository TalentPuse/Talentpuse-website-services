---
description: Dạy một kỹ năng/khái niệm mới trong workspace — load skill `teach`.
agent: build
---

Load skill `teach` (dùng skill tool — skill nằm ở `.agents/skills/teach/`) và dạy chủ đề sau:

$ARGUMENTS

Làm theo đúng cấu trúc dạy trong skill, có ví dụ thực tế từ repo này.
