# Ghim cung combo voi backend (fastmcp 2.11.2 + mcp 1.28.1) — da kiem chung
# chay duoc. Unpinned (`fastmcp>=2.0`) thi pip cai bAN MOI NHAT: mcp SDK moi
# xoa `mcp.shared` -> test chet ModuleNotFoundError va fastmcp import cung co
# the vo (da vap 2026-08-07, CI MCP test job). Nang version thi ghim lai ca cap.
fastmcp==2.11.2
mcp==1.28.1
asyncpg>=0.30
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.0
tenacity>=9.0
python-jobspy>=1.0
pytest>=8.0
pytest-asyncio>=0.24
