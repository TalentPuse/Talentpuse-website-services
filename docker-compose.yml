services:
  frontend:
    build: .
    container_name: tp-frontend-standalone
    ports:
      - "3001:3001"
    environment:
      API_BASE_INTERNAL: ${API_BASE_INTERNAL:-http://localhost:8000}
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-http://localhost:8000}
    restart: unless-stopped
