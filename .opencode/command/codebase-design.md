---
description: Thiết kế/cải thiện deep module theo shared vocabulary — load skill `codebase-design`.
agent: build
---

Load skill `codebase-design` (dùng skill tool — skill nằm ở `.agents/skills/codebase-design/`) và thiết kế/cải thiện module:

$ARGUMENTS

Dùng đúng từ vựng trong skill (deep module, seam, interface...) và chỉ rõ quyết định thiết kế.
