---
description: Dựng prototype dùng một lần để trả lời câu hỏi thiết kế — load skill `prototype`.
agent: build
---

Load skill `prototype` (dùng skill tool — skill nằm ở `.agents/skills/prototype/`) và dựng prototype cho:

$ARGUMENTS

Prototype dùng một lần, tập trung trả lời câu hỏi thiết kế, không cần hoàn thiện.
