---
description: Biến cuộc trò chuyện/hiểu biết hiện tại thành spec và publish lên issue tracker — load skill `to-spec`.
agent: build
---

Load skill `to-spec` (dùng skill tool — skill nằm ở `.agents/skills/to-spec/`) và chuyển nội dung sau thành spec:

$ARGUMENTS

Không phỏng vấn thêm — tổng hợp những gì đã thảo luận rồi publish spec.
