---
description: Đưa issue/PR qua state machine triage (categorise → verify → grill → brief) — load skill `triage`.
agent: build
---

Load skill `triage` (dùng skill tool — skill nằm ở `.agents/skills/triage/`) và xử lý issue/PR sau:

$ARGUMENTS

Làm đúng các vai triage trong skill, kết thúc bằng agent-ready brief.
