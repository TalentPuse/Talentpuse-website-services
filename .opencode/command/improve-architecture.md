---
description: Scan codebase tìm cơ hội deepen, chọn 1 cái rồi grill qua — load skill `improve-codebase-architecture`.
agent: build
---

Load skill `improve-codebase-architecture` (dùng skill tool — skill nằm ở `.agents/skills/improve-codebase-architecture/`) và quét:

$ARGUMENTS

Trình bày báo cáo HTML các cơ hội deepen, chọn cái đáng giá nhất, rồi grill qua trước khi làm.
