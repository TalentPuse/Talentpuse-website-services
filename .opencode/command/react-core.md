---
description: Dùng @copilotkit/react-core (provider, hooks, tools phía client) — load skill `react-core`.
agent: build
---

Load skill `react-core` (dùng skill tool — skill nằm ở `.agents/skills/react-core/`) và làm việc với react-core:

$ARGUMENTS

Chọn đúng reference trong skill cho tác vụ: provider setup, useAgent/useThreads, useFrontendTool, useHumanInTheLoop, renderers...
