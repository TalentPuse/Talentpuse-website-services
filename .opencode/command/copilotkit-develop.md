---
description: Xây tính năng AI với CopilotKit v2 — load skill `copilotkit-develop`.
agent: build
---

Load skill `copilotkit-develop` (dùng skill tool — skill nằm ở `.agents/skills/copilotkit-develop/`) và phát triển tính năng:

$ARGUMENTS

Áp dụng đúng API v2 trong skill (chat interfaces, frontend tools, context sharing, interrupts).
