---
description: Lập bản đồ cho khối lượng công việc lớn thành chuỗi decision tickets — load skill `wayfinder`.
agent: build
---

Load skill `wayfinder` (dùng skill tool — skill nằm ở `.claude/skills/wayfinder/`) và lập map cho:

$ARGUMENTS

Tạo bản đồ decision tickets trên tracker, resolve từng cái cho tới khi đường đi rõ.
