---
description: Xây/chốt domain model và ubiquitous language cho project — load skill `domain-modeling`.
agent: build
---

Load skill `domain-modeling` (dùng skill tool — skill nằm ở `.agents/skills/domain-modeling/`) và làm việc với domain model:

$ARGUMENTS

Chốt terminology + ghi ADR nếu cần, theo đúng workflow trong skill.
