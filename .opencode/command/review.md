---
description: Review code theo 2 trục Standards + Spec — load skill `code-review`.
agent: build
---

Load skill `code-review` (dùng skill tool — skill nằm ở `.claude/skills/code-review/`) và review theo đúng quy trình trong đó.

Đối tượng review: $ARGUMENTS

Nếu không chỉ rõ điểm mốc, review từ mốc gần nhất (commit/branch) và báo cáo 2 bên: Standards và Spec.
