---
description: Phỏng vấn mài plan + tạo ADR và glossary trên đường đi — load skill `grill-with-docs`.
agent: build
---

Load skill `grill-with-docs` (dùng skill tool — skill nằm ở `.agents/skills/grill-with-docs/`) và grill kế hoạch:

$ARGUMENTS

Vừa hỏi vừa ghi ADR + glossary theo quy trình trong skill.
