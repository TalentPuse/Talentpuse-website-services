---
description: Xây backend agent theo giao thức AG-UI — load skill `copilotkit-agui`.
agent: build
---

Load skill `copilotkit-agui` (dùng skill tool — skill nằm ở `.agents/skills/copilotkit-agui/`) và làm việc với AG-UI:

$ARGUMENTS

Áp dụng đúng protocol: event types, SSE transport, AbstractAgent/HttpAgent, state sync, human-in-the-loop.
