---
description: Chẩn đoán bug/regression theo vòng lặp có hệ thống — load skill `diagnosing-bugs`.
agent: build
---

Load skill `diagnosing-bugs` (dùng skill tool — skill nằm ở `.agents/skills/diagnosing-bugs/`) và chẩn đoán vấn đề sau:

$ARGUMENTS

Đi theo vòng lặp trong skill: tìm hiểu → giả thuyết → bằng chứng → fix → verify. Đừng đoán.
