---
description: Stress-test một plan/quyết định bằng cách hỏi dồn dập — load skill `grilling`.
agent: build
---

Load skill `grilling` (dùng skill tool — skill nằm ở `.agents/skills/grilling/`) và grill kế hoạch/quyết định sau:

$ARGUMENTS

Hỏi liên tục cho tới khi tìm ra lỗ hổng hoặc xác nhận plan vững. Đừng chiều.
