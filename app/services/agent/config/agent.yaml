llm:
  model: "${CHAT_MODEL}"
  base_url: "${OPENAI_BASE_URL}"
  temperature: 0.7
  max_tokens: 2048

mcp:
  server_url: "${MCP_SERVER_URL}"
  timeout: 30

agent:
  max_history_messages: 20
  tools:
    - query_skill_gap
    - get_user_profile
