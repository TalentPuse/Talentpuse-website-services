services:
  backend:
    build: .
    container_name: talentpulse-dashboard-api
    ports:
      - "8001:8001"
    environment:
      DATABASE_URL: postgresql://metabase_ro:metabase_ro@postgres:5432/warehouse
    networks:
      - default
      - pipeline_net
    restart: unless-stopped


networks:
  default:
  pipeline_net:
    external: true
    name: pipeline_data_default
