---
description: Đóng góp code lên CopilotKit open-source — load skill `copilotkit-contribute`.
agent: build
---

Load skill `copilotkit-contribute` (dùng skill tool — skill nằm ở `.agents/skills/copilotkit-contribute/`) và đóng góp:

$ARGUMENTS

Làm đúng flow trong skill: fork → clone monorepo → branch → test → PR theo guidelines.
