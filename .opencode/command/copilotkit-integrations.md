---
description: Nối agent framework ngoài (LangGraph, CrewAI, PydanticAI...) vào CopilotKit — load skill `copilotkit-integrations`.
agent: build
---

Load skill `copilotkit-integrations` (dùng skill tool — skill nằm ở `.claude/skills/copilotkit-integrations/`) và tích hợp framework:

$ARGUMENTS

Chọn đúng reference trong skill cho framework được nhắc tới, wire qua AG-UI protocol.
