---
description: Refresh các skill CopilotKit khi kiến thức bị stale — load skill `copilotkit-self-update`.
agent: build
---

Load skill `copilotkit-self-update` (dùng skill tool — skill nằm ở `.agents/skills/copilotkit-self-update/`) và cập nhật các skill CopilotKit trong workspace này.

Lý do / bối cảnh: $ARGUMENTS

Lưu ý: skill này chỉ refresh các SKILL.md của CopilotKit, không đụng vào code project.
