---
description: Thêm CopilotKit vào project hoặc bootstrap từ đầu — load skill `copilotkit-setup`.
agent: build
---

Load skill `copilotkit-setup` (dùng skill tool — skill nằm ở `.agents/skills/copilotkit-setup/`) và setup CopilotKit cho:

$ARGUMENTS

Làm theo đúng skill: detect framework → cài package → runtime wiring → provider → chat chạy được đầu tiên.
