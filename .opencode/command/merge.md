---
description: Giải quyết merge/rebase conflict đang dở — load skill `resolving-merge-conflicts`.
agent: build
---

Load skill `resolving-merge-conflicts` (dùng skill tool — skill nằm ở `.agents/skills/resolving-merge-conflicts/`) và xử lý conflict đang dở trong repo.

Bối cảnh: $ARGUMENTS

Xong phải báo trạng thái git: đã resolve hết conflict chưa, còn bước nào cần người dùng xác nhận.
