---
description: Tách plan/spec thành các ticket (tracer bullets) có khai báo blocking edges — load skill `to-tickets`.
agent: build
---

Load skill `to-tickets` (dùng skill tool — skill nằm ở `.agents/skills/to-tickets/`) và tách nội dung sau thành bộ ticket:

$ARGUMENTS

Mỗi ticket phải khai báo rõ edge blocking. Publish theo tracker đã cấu hình.
