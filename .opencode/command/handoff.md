---
description: Nén cuộc trò chuyện hiện tại thành handoff doc cho agent khác — load skill `handoff`.
agent: build
---

Load skill `handoff` (dùng skill tool — skill nằm ở `.agents/skills/handoff/`) và tạo handoff document cho:

$ARGUMENTS

Mục tiêu: agent khác đọc xong là tiếp tục được ngay, không cần hỏi lại.
