services:
  frontend:
    build: .
    container_name: tp-frontend-standalone
    ports:
      - "8002:8002"
    environment:
      API_BASE_INTERNAL: ${API_BASE_INTERNAL:-http://localhost:8001}
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-http://localhost:8001}
    restart: unless-stopped
