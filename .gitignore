node_modules/
.venv/
__pycache__/
*.pyc
# Moi bien the .env, KHONG chi `.env`. Truoc day chi co dong `.env` nen
# `.env.bak-before-dburl-fix` (chua JWT_SECRET, token Telegram, khoa Resend,
# khoa Tailscale, webhook secret) nam untracked — chi cach mot lenh
# `git add .` la bi commit. Xem JA-04 trong docs/superpowers/bugs/.
.env*
!.env.example
.next/
.pytest_cache/
.ruff_cache/

# Log truy cap nginx ghi ra host (docker-compose mount ./logs/nginx).
# Day la du lieu chay that, khong bao gio duoc commit.
logs/
